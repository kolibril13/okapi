{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.js --minify --format=esm --bundle --outdir=src/okapi/static"
	},
	"dependencies": {
		"p5": "^1.9.4"
	},
	"devDependencies": {
		"esbuild": "^0.21.5"
	}
}
